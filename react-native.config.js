module.exports = {
  // Font files are linked into both native projects by `npx react-native-asset`.
  // Re-run that command after adding or removing a file here.
  assets: ['./src/assets/fonts'],
};
